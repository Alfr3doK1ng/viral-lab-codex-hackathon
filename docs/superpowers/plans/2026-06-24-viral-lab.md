# Viral Lab Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a local browser app that analyzes a TikTok reference, extracts viral DNA, runs an agent committee, generates a remix brief, and produces a playable video preview with a real-provider adapter boundary.

**Architecture:** Use Vite, React, TypeScript, and deterministic domain modules. Keep creative intelligence in pure functions with unit tests, then wire the workflow into a dense control-room UI and verify the full path with Playwright.

**Tech Stack:** Vite, React 18, TypeScript, Vitest, Testing Library, Playwright, lucide-react, browser Canvas/MediaRecorder for offline preview video.

## Global Constraints

- Use deterministic local logic for the first version.
- Keep provider calls behind an interface.
- Do not require backend services unless needed to protect provider keys for real generation calls.
- Keep all local key files ignored by git and safe to remove before submitting the project for judging.
- The app runs locally without API keys using the offline preview provider.
- The app can generate or display a playable final video artifact in the local demo.
- URL validation must allow `https://` TikTok URLs and reject empty or non-URL values.
- Storyboard must contain exactly five shots.
- The visible UI must include spec-first framing, agent committee, scoring, generation, and QA.

---

## File Structure

- Create `package.json`: scripts and dependencies.
- Create `index.html`, `vite.config.ts`, `tsconfig.json`, `tsconfig.node.json`, `playwright.config.ts`, `vitest.setup.ts`: app/test tooling.
- Create `.gitignore` and `.env.example`: keep local keys and generated artifacts out of git.
- Create `src/domain/reference.ts`: reference types, normalization, validation, export bundle type.
- Create `src/domain/scoring.ts`: deterministic viralness dimensions and aggregate analysis.
- Create `src/domain/committee.ts`: deterministic committee findings.
- Create `src/domain/brief.ts`: five-shot remix brief and export JSON builder.
- Create `src/providers/generation.ts`: provider types, browser offline video preview, generic real-provider API handoff.
- Create `src/app/App.tsx`: workflow state and orchestration.
- Create `src/app/components/*.tsx`: intake, analysis, committee, brief, video, export panels.
- Create `src/app/styles.css`: responsive control-room visual system.
- Create `src/main.tsx`: React entrypoint.
- Create `tests/unit/*.test.ts`: unit tests for all domain/provider behavior.
- Create `tests/e2e/viral-lab.spec.ts`: browser e2e for the complete demo path.

## Task 1: Scaffold App, Tooling, And Secret Hygiene

**Files:**
- Create: `package.json`
- Create: `index.html`
- Create: `vite.config.ts`
- Create: `tsconfig.json`
- Create: `tsconfig.node.json`
- Create: `playwright.config.ts`
- Create: `vitest.setup.ts`
- Create: `.gitignore`
- Create: `.env.example`
- Create: `src/main.tsx`
- Create: `src/app/App.tsx`
- Create: `src/app/styles.css`

**Interfaces:**
- Produces: `npm run dev`, `npm run test`, `npm run test:e2e`, `npm run build`.
- Produces: `.env.local` and `local.keys.json` are ignored.

- [ ] **Step 1: Write the initial smoke test**

Create `tests/unit/app-smoke.test.ts`:

```ts
import { describe, expect, it } from "vitest";

describe("Viral Lab scaffold", () => {
  it("uses the expected app name", () => {
    expect("Viral Lab").toBe("Viral Lab");
  });
});
```

- [ ] **Step 2: Run test to verify tooling is missing**

Run: `npm test -- --run tests/unit/app-smoke.test.ts`

Expected: FAIL because `package.json` and Vitest do not exist yet.

- [ ] **Step 3: Add Vite/React/TypeScript tooling**

Create the config files and scripts:

```json
{
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc -b && vite build",
    "test": "vitest",
    "test:run": "vitest run",
    "test:e2e": "playwright test",
    "preview": "vite preview --host 127.0.0.1"
  }
}
```

Add `.gitignore` entries:

```gitignore
node_modules
dist
playwright-report
test-results
.env
.env.local
.env.*.local
local.keys.json
*.local.json
```

- [ ] **Step 4: Install dependencies and verify smoke test passes**

Run: `npm install`

Run: `npm run test:run -- tests/unit/app-smoke.test.ts`

Expected: PASS with one test.

- [ ] **Step 5: Commit**

Run:

```bash
git add .
git commit -m "chore: scaffold Viral Lab app"
```

## Task 2: Reference Validation And Viralness Scoring

**Files:**
- Create: `src/domain/reference.ts`
- Create: `src/domain/scoring.ts`
- Create: `tests/unit/reference.test.ts`
- Create: `tests/unit/scoring.test.ts`

**Interfaces:**
- Produces: `validateReferenceInput(input: ReferenceInput): ValidationResult<NormalizedReference>`
- Produces: `analyzeViralness(reference: NormalizedReference): ViralAnalysis`
- Consumes: Types `ReferenceInput`, `NormalizedReference`, `ViralAnalysis`, `ViralDimension`.

- [ ] **Step 1: Write failing reference validation tests**

Test valid TikTok URLs, invalid non-HTTPS URLs, insufficient text, missing target niche, numeric metrics, and upload metadata.

Run: `npm run test:run -- tests/unit/reference.test.ts`

Expected: FAIL because `src/domain/reference.ts` does not exist.

- [ ] **Step 2: Implement reference types and validation**

Implement:

```ts
export function validateReferenceInput(input: ReferenceInput): ValidationResult<NormalizedReference> {
  const errors: FieldError[] = [];
  const url = input.url.trim();
  const caption = input.caption.trim();
  const transcript = input.transcript.trim();
  const targetNiche = input.targetNiche.trim();

  if (!isHttpsTikTokUrl(url)) errors.push({ field: "url", message: "Enter a valid https TikTok URL." });
  if ((caption + transcript).replace(/\s/g, "").length < 30) errors.push({ field: "transcript", message: "Add at least 30 characters across caption and transcript." });
  if (!targetNiche) errors.push({ field: "targetNiche", message: "Choose the audience you want the new video to reach." });

  if (errors.length > 0) return { ok: false, errors };
  return { ok: true, value: normalizeReference(input) };
}
```

- [ ] **Step 3: Write failing viralness tests**

Test deterministic output, score range 0-100, all seven required dimensions, and remix constraints against copying.

Run: `npm run test:run -- tests/unit/scoring.test.ts`

Expected: FAIL because `analyzeViralness` does not exist.

- [ ] **Step 4: Implement deterministic scoring**

Implement weighted heuristics using transcript/caption/metrics:

```ts
const DIMENSION_IDS = [
  "hook-clarity",
  "payoff-density",
  "emotional-contrast",
  "visual-specificity",
  "remixability",
  "social-proof",
  "retention-cues"
] as const;
```

Each dimension returns `{ id, label, score, rationale }`. Aggregate score is the rounded average clamped to 0-100.

- [ ] **Step 5: Verify unit tests pass**

Run: `npm run test:run -- tests/unit/reference.test.ts tests/unit/scoring.test.ts`

Expected: PASS.

- [ ] **Step 6: Commit**

Run:

```bash
git add src/domain/reference.ts src/domain/scoring.ts tests/unit/reference.test.ts tests/unit/scoring.test.ts
git commit -m "feat: add reference validation and viralness scoring"
```

## Task 3: Agent Committee, Remix Brief, And Export Bundle

**Files:**
- Create: `src/domain/committee.ts`
- Create: `src/domain/brief.ts`
- Create: `tests/unit/committee.test.ts`
- Create: `tests/unit/brief.test.ts`

**Interfaces:**
- Consumes: `NormalizedReference`, `ViralAnalysis`.
- Produces: `runAgentCommittee(reference, analysis): CommitteeFinding[]`
- Produces: `generateRemixBrief(reference, analysis, committee): RemixBrief`
- Produces: `createExportBundle(reference, analysis, committee, brief, videoState): ExportBundle`

- [ ] **Step 1: Write failing committee tests**

Assert that the committee contains Analyst, Strategist, Director, Prompt Engineer, and QA Critic. Assert each has verdict, confidence 0-100, and at least three findings.

Run: `npm run test:run -- tests/unit/committee.test.ts`

Expected: FAIL because committee module does not exist.

- [ ] **Step 2: Implement committee generation**

Generate deterministic findings from analysis dimensions, viral DNA, source niche, and target niche. QA Critic must include originality and safety findings.

- [ ] **Step 3: Write failing brief/export tests**

Assert exactly five shots, each with timing/visual/camera/on-screen text/audio cue/image prompt. Assert negative prompts reject copying. Assert export bundle includes input summary, analysis, committee, brief, and video state.

Run: `npm run test:run -- tests/unit/brief.test.ts`

Expected: FAIL because brief module does not exist.

- [ ] **Step 4: Implement brief and export generation**

Use the target niche in title, premise, hook, five-shot storyboard, Seedance-style video prompt, Image 2 prompts, negative prompts, captions, hashtags, and readiness checklist.

- [ ] **Step 5: Verify unit tests pass**

Run: `npm run test:run -- tests/unit/committee.test.ts tests/unit/brief.test.ts`

Expected: PASS.

- [ ] **Step 6: Commit**

Run:

```bash
git add src/domain/committee.ts src/domain/brief.ts tests/unit/committee.test.ts tests/unit/brief.test.ts
git commit -m "feat: add committee and remix brief generation"
```

## Task 4: Video Generation Provider Boundary

**Files:**
- Create: `src/providers/generation.ts`
- Create: `tests/unit/generation.test.ts`

**Interfaces:**
- Consumes: `RemixBrief`.
- Produces: `generateOfflinePreviewVideo(brief: RemixBrief): Promise<VideoGenerationResult>`
- Produces: `requestRealProviderGeneration(brief: RemixBrief): Promise<VideoGenerationResult>`
- Produces: `generateVideo(brief: RemixBrief, mode?: "auto" | "offline" | "real"): Promise<VideoGenerationResult>`

- [ ] **Step 1: Write failing provider tests**

Test that offline provider returns `succeeded`, provider `Offline preview`, a non-empty video URL or result, and provider-safe diagnostics. Test that real provider gracefully returns `failed` or `queued` without exposing secrets when unavailable.

Run: `npm run test:run -- tests/unit/generation.test.ts`

Expected: FAIL because provider module does not exist.

- [ ] **Step 2: Implement provider types and offline test fallback**

Implement provider state types and a deterministic offline result object. In browser runtime, create a WebM preview using Canvas and MediaRecorder. In unit tests or unsupported browsers, return a deterministic data URL result and diagnostics that state preview rendering is unavailable in the test runtime.

- [ ] **Step 3: Implement real provider handoff**

Implement a client handoff to `/api/generate-video` that sends prompt data without secrets. If unavailable, return a failed provider state with a safe message. Keep `.env.local` and `local.keys.json` ignored.

- [ ] **Step 4: Verify provider tests pass**

Run: `npm run test:run -- tests/unit/generation.test.ts`

Expected: PASS.

- [ ] **Step 5: Commit**

Run:

```bash
git add src/providers/generation.ts tests/unit/generation.test.ts
git commit -m "feat: add video generation provider boundary"
```

## Task 5: Build The Viral Lab UI

**Files:**
- Modify: `src/app/App.tsx`
- Modify: `src/app/styles.css`
- Create: `src/app/components/ReferenceIntake.tsx`
- Create: `src/app/components/AnalysisPanel.tsx`
- Create: `src/app/components/CommitteePanel.tsx`
- Create: `src/app/components/BriefPanel.tsx`
- Create: `src/app/components/VideoPanel.tsx`
- Create: `src/app/components/ExportPanel.tsx`
- Modify: `src/main.tsx`

**Interfaces:**
- Consumes: domain and provider functions from Tasks 2-4.
- Produces: complete browser workflow from intake to video preview and JSON export.

- [ ] **Step 1: Write failing UI smoke test**

Create a Testing Library test that renders `<App />`, expects "Viral Lab", "Spec-first loop", and "Analyze viral DNA".

Run: `npm run test:run -- tests/unit/app-ui.test.tsx`

Expected: FAIL because the UI does not contain the workflow yet.

- [ ] **Step 2: Implement app workflow state**

State includes reference input, validation errors, analysis, committee, brief, video state, copy/export status, and in-progress flags.

- [ ] **Step 3: Implement components**

Use compact panels:

```txt
desktop: intake | analysis + committee | brief + video + export
mobile: stacked intake -> analysis -> committee -> brief -> video -> export
```

Buttons:
- **Analyze viral DNA**
- **Generate remix brief**
- **Generate video**
- **Copy JSON**

- [ ] **Step 4: Implement distinctive visual system**

Use dark ink app chrome, paper output panels, restrained scoring colors, monospace prompt blocks, and the viral DNA strip as the signature visual element.

- [ ] **Step 5: Verify UI test and build pass**

Run: `npm run test:run -- tests/unit/app-ui.test.tsx`

Run: `npm run build`

Expected: PASS.

- [ ] **Step 6: Commit**

Run:

```bash
git add src tests/unit/app-ui.test.tsx
git commit -m "feat: build Viral Lab workflow UI"
```

## Task 6: Browser E2E And Demo Verification

**Files:**
- Create: `tests/e2e/viral-lab.spec.ts`
- Modify: `playwright.config.ts`

**Interfaces:**
- Consumes: app scripts and UI from Task 5.
- Produces: verified browser happy path at desktop and mobile widths.

- [ ] **Step 1: Write failing e2e test**

Test:
- open `/`
- fill TikTok URL, caption, transcript, source niche, target niche, and metrics
- click **Analyze viral DNA**
- expect the viral DNA strip and five committee agents
- click **Generate remix brief**
- expect exactly five storyboard shots
- click **Generate video**
- expect a `<video>` or labeled video preview result
- click **Copy JSON**
- expect copy/export confirmation

Run: `npm run test:e2e`

Expected: FAIL until UI selectors and preview generation are complete.

- [ ] **Step 2: Implement any missing selectors and stable labels**

Use accessible labels and `data-testid` only where accessible queries are insufficient.

- [ ] **Step 3: Run full verification**

Run:

```bash
npm run test:run
npm run build
npm run test:e2e
```

Expected: all commands exit 0.

- [ ] **Step 4: Manual browser verification**

Start the dev server:

```bash
npm run dev -- --port 5173
```

Open desktop and mobile viewports with Playwright or the in-app browser. Verify:
- no blank page
- no overlapping text
- generated video panel visible after generation
- five storyboard shots render
- JSON export includes provider state

- [ ] **Step 5: Commit**

Run:

```bash
git add .
git commit -m "test: add e2e verification for Viral Lab"
```

## Plan Self-Review

- Spec coverage: reference intake, TikTok URL, viralness scoring, agent committee, remix brief, final video path, provider boundary, JSON export, desktop/mobile UX, unit tests, and e2e tests are covered.
- Red-flag scan: no incomplete-work markers or undefined future work remains in required implementation steps.
- Type consistency: `ReferenceInput`, `NormalizedReference`, `ViralAnalysis`, `CommitteeFinding`, `RemixBrief`, and `VideoGenerationState` flow consistently across tasks.
